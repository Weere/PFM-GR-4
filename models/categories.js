class Category {
    constructor(id, category, intialAmount, items, amount, balance, totalAmount) {
      this.id = id;
      this.category = category;
      this.intialAmount = intialAmount;
      this.items = items;
      this.amount = amount;
      this.balance = balance;
      this.totalAmount = totalAmount;
    }
  }
  
  export default Category;
  